# requirements.in — fuente de verdad; requirements.txt es generado con:
#   pip-compile requirements.in -o requirements.txt
Flask>=3.0.0,<4.0.0
Werkzeug>=3.0.0,<4.0.0
pandas>=2.1.0,<3.0.0
numpy>=1.24.0,<3.0.0
python-dateutil>=2.8.2
openpyxl>=3.1.0,<4.0.0
Flask-Caching>=2.1.0,<3.0.0

# Opcional: si está disponible, pandas la usa para to_json y otras rutas de
# escritura masiva. No requerido en runtime, pero su presencia mejora la
# velocidad de descarga en datasets grandes.
pyarrow>=14.0.0,<22.0.0 ; python_version >= "3.10"

# --- Solo para tests/lint locales (no usado en producción) ---
pytest>=8.0.0,<10.0.0
pytest-cov>=5.0.0,<8.0.0
ruff>=0.5.0,<1.0.0
black>=24.0.0,<26.0.0
